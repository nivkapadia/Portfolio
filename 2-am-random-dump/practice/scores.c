#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int score1 = 72;
    int score2 = 73;
    int score4 = 33;

    printf("Average: %f\n", (score1 + score2 + score3) / 3.0)
    //so long as any one of the value participating in an arithmetic expression is something like a float, the rest will be treated as float as well.
}